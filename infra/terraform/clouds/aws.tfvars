# Platform overlay: AWS. Cloud-wide, env- and client-agnostic. Select with `deploy.sh --cloud aws`.
# You never edit this to switch clouds — you just pick a different overlay.
cloud_provider = "aws"
region         = "us-east-1"
