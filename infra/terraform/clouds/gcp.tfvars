# Platform overlay: Google Cloud. Select with `deploy.sh --cloud gcp`.
# Set gcp_project once to your project id.
cloud_provider = "gcp"
region         = "us-central1"
gcp_project    = "kita-platform"
