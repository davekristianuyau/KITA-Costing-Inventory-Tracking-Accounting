// Gateway module build (SCAFFOLDING SKELETON).
// Spring Cloud Gateway service — routes /api/* to backend microservices.
// Dependencies declared as intent; no application code yet.

plugins {
    java
    // id("org.springframework.boot")
    // id("io.spring.dependency-management")
}

dependencies {
    // implementation("org.springframework.cloud:spring-cloud-starter-gateway")
    // implementation("org.springframework.boot:spring-boot-starter-actuator")
    // testImplementation("org.springframework.boot:spring-boot-starter-test")
}

// The Spring Cloud BOM and Boot plugin are wired in when the service is implemented.
