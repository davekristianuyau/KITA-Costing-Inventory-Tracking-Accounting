# Gateway configuration. Routes are defined in RoutesConfig (Java); targets are env-overridable there.
server:
  port: 8081

spring:
  application:
    name: gateway

management:
  endpoints:
    web:
      exposure:
        include: health,info
